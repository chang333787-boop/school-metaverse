# 우리학교 메타버스 — 프로젝트 가이드

초등 4학년 5명과 함께 만드는 3인칭 3D 학교 맵. 이 파일은 어느 컴퓨터에서든 Claude가 이 프로젝트를 이어받기 위한 단일 안내서다. 수업 중이라면 아래 "수업 라이브 프로토콜"을 최우선으로 따른다.

## 프로젝트 개요
- 수업 콘셉트: 아이들이 자료(사진·측량·도면·요청)를 입력 → Claude가 반영 → push → 새로고침으로 변화를 확인하는 반복 루프
- 배포: main에 push하면 GitHub Pages 자동 배포(1~2분) → https://chang333787-boop.github.io/school-metaverse/
- 학생 접속: 크롬북에서 위 링크. 갱신이 안 보이면 `Ctrl+Shift+R`
- 기술: three.js r170 로컬 번들(lib/), 외부 네트워크 의존 0, 빌드 과정 없음

## 투트랙 구조 (2026-07-30 확정)
- 루트(`index.html`, `js/`, `lib/`) = **발전판** — 모든 개발·실험은 여기서만
- `classroom/` = **아이들 버전 v0.8 동결 스냅샷** (2026-07-28 수업 결과물 · git tag `v0.8-classroom` · URL https://chang333787-boop.github.io/school-metaverse/classroom/)
- **`classroom/` 폴더는 어떤 이유로도 수정·삭제하지 않는다** (버스터·리팩터링·일괄 치환에서 항상 제외)

## 파일 구조와 수정 지점
| 파일 | 역할 |
|---|---|
| `js/data.js` | **모든 배치의 단일 수정 지점** — 학교명, 건물 크기, 층·실 배열, 시설 위치 |
| `js/world.js` | data.js를 3D로 생성 (벽·문·팻말·가구 자동). 새 오브젝트(요청 반영)는 이 파일 하단에 추가 |
| `js/player.js` | 캐릭터·물리 (보통 수정 불필요) |
| `js/main.js` | 카메라·HUD·디버그 API(`window.SD`) |
| `js/textures.js` | 캔버스 텍스처 — 팻말은 `textSign`(양면 정상 표시) |

## data.js 규약 (v0.5 — 교사 주석으로 복도·문·마당 확정)
- 좌표: x=동서(동쪽+), z=남북(남쪽+), 단위 미터. 실 `span=[x0,x1]`은 월드 좌표 x 구간. 실 폭은 배치도 픽셀 실측 비율
- `building.front`: 앞줄(원무실~3학년, 1층만) — 문은 북측 주 복도로, 현관은 남쪽 바깥과 연결. westDoor/eastDoor = 바깥문 z
- `building.wings[]`: 주 복도에 바로 접한 서관만 (twoStory). `innerOnly: true`(문서고)는 복도 문 없이 옆방(나래반)에서 진입
- `building.kitchen`: 급식동 — 문은 동쪽 세로복도(doorZ), 뒤쪽 바깥문(backDoorX)
- `building.linkCorridor`: 현관 북쪽 세로복도 → 동관 복도와 연결
- `building.eastWing`: 동관(2·4학년·과학실·창고) — **앞줄과의 사이는 야외 마당**. 복도는 건물 북측, 방 문은 북쪽으로. yardDoor = 주복도→마당 문 x
- `building.upper`: 서관 위 2층(6학년|5학년|소담실). 계단실(type 'stair') 경사로로 연결 — 경사로 동측, 2층 진입 통로 서측 1.5m
- room `type`: classroom / office / nurse / cafeteria / library / science / computer / hall / toilet / storage / stair / daycare
- 가구는 방 상대 좌표 자동 배치(furnish) — 방 폭·깊이 바뀌어도 벽 뚫림 없음

## 검증 루틴 (push 전 필수)
1. 문법: `node --input-type=module --check < js/수정파일.js`
2. 로컬 육안 1회: `python3 -m http.server 8001` (윈도우: `py -m http.server 8001`, 또는 VS Code Live Server) → 콘솔 에러 0 확인
3. 이동 테스트 필요 시 콘솔에서 `SD.tp(x,z,y)` `SD.step(n,['KeyW'])` `SD.zone()` 사용
4. 주의: 브라우저 탭이 백그라운드면 rAF가 멈춰 화면이 정지된 것처럼 보인다 (버그 아님)

## 멀티 세션 역할 (교사가 첫마디로 "역할: ○○"을 주면 그 역할만 수행)
- **역할: 공사반장** — 유일하게 코드를 수정·commit·push하는 세션. `[측량]` `[도면]` `[반영해줘]`와 채택된 명령문을 받아 반영한다. 보고는 "push 완료 — 1~2분 뒤 새로고침!" 한 줄. 다른 역할 세션이 떠 있어도 코드 편집은 이 세션만 한다
- **역할: 통역가** — `[소원-이름] 원문`을 명령문으로 통역하고 아이와 확인 대화만 한다. **파일 수정·git 조작 금지.** 채택된 명령문은 교사가 공사반장에게 전달한다. 화면이 투사되는 메인 세션이므로 답은 특히 짧고 아이가 읽기 쉽게
- **역할: 박사님** — 아이 질문 응대(아래 "아이 질문 응대" 모드), 사진 판정(✅/🔄), 아이가 직접 쓴 프롬프트의 통과/보완 판정. **파일 수정·git 조작 금지**
- 역할 지정 없이 시작된 세션은 전체 프로토콜을 단독 수행한다

## 수업 라이브 프로토콜
교사가 던지는 메시지 형식과 응답 방식:
- `[소원-이름] 원문` → ①원문을 **무엇을·어디에·어떤 모습·얼마나 크게**가 담긴 명령문으로 통역해 짧게 제시 ②아이에게 "네 생각과 같아요?" 확인 ③교사가 "채택"하면 반영. 완성물 옆에 "○○의 △△" 이름 팻말을 세운다
- `[측량] 수치/캡처` `[도면] 사진` → data.js에 반영
- `[질문-이름] 내용` → 아래 "아이 질문 응대" 모드로
- `[교사] 내용` → 교사 상대 일반 모드
- 반영 완료 보고는 한 줄만: "push 완료 — 1~2분 뒤 다같이 새로고침!"
- **수업 중 모든 답변은 짧게.** 화면이 아이들에게 투사되고 있다
- **AI 예산제**: 학급 공동 10회. 데이터 제출·질문은 무료, 구현 요청만 1회 차감. 교사가 잔여 횟수를 알려주면 답 끝에 "(n회 남음)" 표기
- 사진 판정 요청 시: `✅ 쓸 수 있어요!` / `🔄 정면에서 다시 찍으면 더 좋아요` 수준으로 짧게
- 프롬프트 통역은 1인 2회까지. 3회째부터는 아이가 직접 쓴 명령문을 "통과/보완할 점" 판정

## 아이 질문 응대 (학생이 직접 질문할 때)
- 쉬운 해요체, 2~4문장, 이모지는 절제
- 지금 받은 질문에 처음 답하는 것처럼 신선하게 응대한다 (수업 설계·준비 과정을 화면에서 언급하지 않는다)
- 정답을 바로 주지 말고 **힌트 + 되묻기**로 아이가 스스로 발견하게 한다. 예: "나는 너희 학교를 본 적이 없어요. 교실이 어떤 순서로 있는지 알 수 있는 그림 같은 게 있을까요?"
- 폭력적·부적절한 요청은 부드럽게 다른 아이디어로 전환한다

## 스타일 가이드
- **실사 금지** — 사진은 참고자료로 보고 low-poly로 재해석한다 ("마인크래프트로 지은 우리 학교" 느낌)
- 인식 포인트 우선순위: ①외벽 색 조합 ②건물 실루엣·층수 ③교문→현관 등굣길 배치 ④랜드마크 ⑤실제 실 이름
- 확인된 랜드마크: **거대한 나무**(상징물로 크게), **큰 텃밭**, **숲놀이터**(통나무형으로 재건축)
- 밝고 순한 색, 저학년 친화. 기존 재질·팻말 스타일과 통일

## 성능 예산 (기능 추가 시 커밋 조건)

### 매 프레임 코드 금지 사항
- 전체 배열 순회·전체 레이캐스트 금지 → 반드시 `world.grid`(8m 격자)로 좁힌다
- 새 객체 생성 금지 (`new THREE.Vector3` 등) → 모듈 상단 스크래치 재사용
- `localStorage` 쓰기 금지 (2초 간격 상한)

### 오브젝트 생성 규칙
- 상자·면은 `box()`/`plane()` 헬퍼 경유 (공유 UNIT_BOX/UNIT_PLANE + scale)
- 재질은 `mat()` 캐시 경유. `new THREE.Material` 직접 호출 금지
- 캔버스 텍스처는 캐시 경유 (`textSign`은 자동 캐시)
- 정적 오브젝트는 `matrixAutoUpdate = false` (buildWorld 끝에서 일괄) + `world.grid` 등록
- **조명(Light)을 새로 추가하지 않는다.** 밝게 보여야 하면 MeshBasicMaterial(형광등 `lamp()`)을 쓴다

### 커밋 전 `SD.perf()` 확인
fps ≥ 30 / rayObjs ≤ 80 / drawCalls 관찰. **fps가 무너지면 기능을 빼고 간다.**

## 크래시 대응
- **저장할 때마다 새로고침됨** → VS Code Live Server 자동 새로고침. 버그 아님
- **화면이 얼어붙음** → 컨텍스트 손실 또는 루프 예외. `SD.diag()` 확인
- **탭이 죽고 새로고침됨** → 메모리 부족. `SD.diag().last.mem` 확인
- 크래시 보고를 받으면 **그 크롬북에서 먼저 `SD.diag()`를 받는다** (또 새로고침하기 전에)
- 끼임 좌표는 `SD.diag().stuck`에 자동 기록된다 — 모아서 다음 수정 대상으로

## 수업 전 점검
1. 배포 링크에서 3분 돌아다니기 (복도 완주·계단 2층·문 통과·미끄럼틀·텃밭)
2. `SD.perf()` 예산 내인지
3. `SD.diag()` — `crashes` 0인지 (아니면 원인 확인 후 `SD.diagClear()`)
4. 크롬 탭 최소화 (다른 탭이 메모리를 먹으면 이 탭이 먼저 죽는다)

## NPC·명단을 늘릴 때
사람 1명 = 메시 약 10개 + 이름표 텍스처 1장. 추가 후 반드시 `SD.perf()` 확인.
**대사(lines)는 교사가 승인한 것만 data.js에 넣는다. 임의 생성 금지.**
대사 작성은 아이들이 직접 하게 한다 (수업 활동으로 좋고 AI 예산도 안 든다).

## 개인정보·안전 (공개 저장소임)
- 학생 이름은 성 없이 이름만 팻말에 사용
- **아이들 사진·위성 캡처 원본을 절대 repo에 커밋하지 않는다** — 보고 수치·형태만 코드에 반영
- 실제 학교명을 코드에 넣는 것은 교사 확인 후에만
- 얼굴이 나온 사진은 사용하지 않는다
